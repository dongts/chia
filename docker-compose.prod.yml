services:
  # -------------------------------------------------------------------------
  # PostgreSQL — comment out this entire service if using an external database
  # -------------------------------------------------------------------------
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-chia}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-chia}
      POSTGRES_DB: ${DB_NAME:-chia}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-chia}"]
      interval: 2s
      timeout: 5s
      retries: 10

  # -------------------------------------------------------------------------
  # Backend API
  # -------------------------------------------------------------------------
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      # If DATABASE_URL is set in .env, it takes priority.
      # Otherwise, entrypoint.sh builds it from DB_* vars.
      CHIA_DATABASE_URL: ${DATABASE_URL:-}
      DB_USER: ${DB_USER:-chia}
      DB_PASSWORD: ${DB_PASSWORD:-chia}
      DB_HOST: ${DB_HOST:-db}
      DB_PORT: ${DB_PORT:-5432}
      DB_NAME: ${DB_NAME:-chia}
      USE_CONNECTION_POOLER: ${USE_CONNECTION_POOLER:-false}
      CHIA_JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env}
      CHIA_CORS_ORIGINS: '["${DOMAIN:-http://localhost}"]'
      CHIA_UPLOAD_DIR: /data/uploads
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads:/data/uploads

  # -------------------------------------------------------------------------
  # Frontend (Nginx serves static + reverse proxies API)
  # -------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    ports:
      - "${PORT:-80}:80"
    depends_on:
      - backend

volumes:
  pgdata:
  uploads:
